'use strict';
/* Babybird Promises with the default option set, which is "mostly" ES6
 * compliant, except for exceptionally poorly-behaved ES6 subclasses. */
module.exports = require('./lib/promise.js')({/* Default options */});
